# Пример конфигурации nginx для Onboarding Checklist API
# Скопируйте в /etc/nginx/sites-available/onboarding-api

server {
    listen 80;
    server_name api.your-domain.com;  # Замените на ваш домен или IP

    # Логи
    access_log /var/log/nginx/onboarding-api-access.log;
    error_log /var/log/nginx/onboarding-api-error.log;

    # Основной location для API
    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        proxy_cache_bypass $http_upgrade;
    }

    # Дополнительные настройки безопасности
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# SSL версия (для использования с Let's Encrypt)
# Раскомментируйте после получения SSL сертификата

# server {
#     listen 443 ssl http2;
#     server_name api.your-domain.com;
# 
#     # SSL сертификаты
#     ssl_certificate /etc/letsencrypt/live/api.your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.your-domain.com/privkey.pem;
#     
#     # SSL настройки
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
# 
#     # Логи
#     access_log /var/log/nginx/onboarding-api-access.log;
#     error_log /var/log/nginx/onboarding-api-error.log;
# 
#     location / {
#         proxy_pass http://localhost:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# 
#     # Безопасность
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-XSS-Protection "1; mode=block" always;
# }
# 
# # Редирект с HTTP на HTTPS
# server {
#     listen 80;
#     server_name api.your-domain.com;
#     return 301 https://$server_name$request_uri;
# }

# Инструкция по установке:
# 1. Скопируйте этот файл:
#    sudo cp nginx.conf.example /etc/nginx/sites-available/onboarding-api
#
# 2. Отредактируйте server_name на ваш домен
#
# 3. Создайте симлинк:
#    sudo ln -s /etc/nginx/sites-available/onboarding-api /etc/nginx/sites-enabled/
#
# 4. Проверьте конфигурацию:
#    sudo nginx -t
#
# 5. Перезагрузите nginx:
#    sudo systemctl reload nginx
#
# 6. Для SSL с Let's Encrypt:
#    sudo certbot --nginx -d api.your-domain.com

