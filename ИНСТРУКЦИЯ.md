# Быстрая инструкция по использованию Onboarding Checklist

## Что это?

Сервер для отслеживания прогресса онбординга новых сотрудников. **Две версии:**

1. **HTTP API** (`http_server.py`) - для веб-запросов, nginx, curl
2. **MCP Server** (`server.py`) - для Claude Desktop и MCP клиентов

## Быстрый старт

### HTTP API версия (если нужны HTTP запросы)

```bash
cd /Users/whom/Documents/watsonxMCP
pip install -r requirements.txt
python http_server.py
```

Сервер запустится на **http://localhost:8000**

**Документация API:** http://localhost:8000/docs (Swagger UI)

**Примеры:**
```bash
# Получить прогресс пользователя
curl http://localhost:8000/api/users/user@company.com/progress

# Отметить задачу
curl -X POST http://localhost:8000/api/users/tasks/complete \
  -H "Content-Type: application/json" \
  -d '{"email": "user@company.com", "task_id": 1}'
```

📖 **Полная документация HTTP API:** см. файл [HTTP_API.md](HTTP_API.md)

---

### MCP версия (для Claude Desktop)

```bash
cd /Users/whom/Documents/watsonxMCP
pip install -r requirements.txt
```

### 2. Настройка менторов

Откройте `config.json` и добавьте email менторов:

```json
{
  "mentors": [
    "ваш-email@company.com"
  ]
}
```

### 3. Подключение к Claude Desktop

Откройте файл конфигурации:
- **MacOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

Добавьте:

```json
{
  "mcpServers": {
    "onboarding-checklist": {
      "command": "python",
      "args": ["/Users/whom/Documents/watsonxMCP/server.py"]
    }
  }
}
```

### 4. Настройка агента

Используйте содержимое файла `agent_prompt.txt` как системный промпт для вашего агента.

### 5. Перезапустите Claude Desktop

После добавления конфигурации перезапустите приложение.

## Основные возможности

### Для сотрудников

1. **Посмотреть свой прогресс:**
   - "Покажи мой прогресс онбординга (user@company.com)"
   
2. **Отметить выполненную задачу:**
   - "Я выполнил задачу 1 (user@company.com)"
   - "Отметь задачу 3 как выполненную для user@company.com"

### Для менторов

**Посмотреть прогресс всех сотрудников:**
- "Покажи прогресс всех сотрудников (mentor@company.com)"

## Структура чек-листа (9 задач)

**День 1 (задачи 1-4):**
1. Встреча с менеджером
2. Встреча с buddy/ментором
3. Прочитать справочник компании
4. Пройти базовый тренинг по безопасности

**День 2 (задачи 5-7):**
5. Настроить окружение для разработки
6. Присоединиться к stand-up команды
7. Прочитать документацию основного продукта

**День 3 (задачи 8-9):**
8. Понаблюдать за работой члена команды
9. Взять первую задачу из бэклога

## Файлы проекта

- `http_server.py` - **HTTP API сервер** (FastAPI, порт 8000)
- `server.py` - **MCP сервер** (stdio для Claude Desktop)
- `data.json` - База данных пользователей (общая для обеих версий)
- `config.json` - Конфигурация (список менторов)
- `agent_prompt.txt` - Промпт для MCP агента
- `requirements.txt` - Python зависимости
- `README.md` - Документация MCP версии
- `HTTP_API.md` - **Документация HTTP API версии**
- `nginx.conf.example` - Пример конфигурации nginx

## Важно знать

✅ **Автоматическое создание пользователей** - если email нет в базе, пользователь создается автоматически с нулевым прогрессом

✅ **Защита данных** - только email из списка менторов могут видеть прогресс всех пользователей

✅ **Хранение данных** - все данные в `data.json`, можно делать бэкапы простым копированием файла

## Решение проблем

**Сервер не запускается:**
```bash
# Проверьте установку зависимостей
pip install -r requirements.txt
```

**Агент не видит инструменты:**
- Проверьте путь в конфигурации MCP
- Перезапустите Claude Desktop
- Убедитесь, что Python доступен в PATH

**Ошибка доступа для ментора:**
- Проверьте, что email добавлен в `config.json`
- Email должен точно совпадать (включая регистр)

## Тестирование

После настройки попробуйте:

1. Спросите агента: "Покажи мой прогресс онбординга для test@company.com"
2. Скажите: "Отметь задачу 1 как выполненную для test@company.com"
3. Если вы ментор: "Покажи всех пользователей (ваш-email@company.com)"

## Настройка nginx (для HTTP версии)

1. **Запустите HTTP сервер:**
   ```bash
   python http_server.py
   ```

2. **Скопируйте конфигурацию:**
   ```bash
   sudo cp nginx.conf.example /etc/nginx/sites-available/onboarding-api
   sudo nano /etc/nginx/sites-available/onboarding-api
   # Измените server_name на ваш домен
   ```

3. **Активируйте:**
   ```bash
   sudo ln -s /etc/nginx/sites-available/onboarding-api /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl reload nginx
   ```

Теперь API доступен через nginx на вашем домене! 🚀

## Полезные ссылки

- **HTTP API документация:** [HTTP_API.md](HTTP_API.md)
- **Swagger UI:** http://localhost:8000/docs (после запуска http_server.py)
- **MCP документация:** [README.md](README.md)

Всё готово к использованию! 🎉

